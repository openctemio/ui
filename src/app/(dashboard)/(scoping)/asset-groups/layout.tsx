import { type ReactNode } from 'react'

/**
 * Asset Groups Layout
 *
 * Asset groups allow users to organize assets into logical groups
 * for better management and access control.
 */
export default function AssetGroupsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
