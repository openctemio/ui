import { type ReactNode } from 'react'

/**
 * Agents Layout
 *
 * Agents are required to execute scans.
 */
export default function AgentsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
