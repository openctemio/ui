import { type ReactNode } from 'react'

/**
 * Components Layout
 *
 * Components (SBOM - Software Bill of Materials) track dependencies,
 * licenses, and vulnerabilities in software components.
 */
export default function ComponentsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
